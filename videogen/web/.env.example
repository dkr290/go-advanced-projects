# Web UI Configuration
PORT=3000
GIN_MODE=release

# API Backend URL (the Wan2.1 server we built)
API_BASE_URL=http://localhost:8080

# Upload settings
MAX_UPLOAD_SIZE=100MB
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
ALLOWED_VIDEO_TYPES=mp4,avi,mov,webm

# UI Settings
VIDEOS_PER_PAGE=12
ENABLE_GALLERY=true
